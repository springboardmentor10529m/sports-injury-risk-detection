# Frontend (skeleton)

Simple React skeleton. To run locally:

```bash
cd frontend
npm install
npm start
```

The frontend assumes the backend is available at `/api/*` (same host); configure a proxy in development if needed.
